---
export interface Props {
  src: string;
  alt: string;
}
const { src, alt } = Astro.props as Props;
---
<img src={src} alt={alt} class="s -z-10 inset-x-0 top-0 w-full h-1/2 absolute object-cover">